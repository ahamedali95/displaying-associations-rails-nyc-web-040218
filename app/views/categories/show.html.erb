<h1><%= @category.name %></h1>

<!-- Add code here to list a category's posts. -->
<h3><%= pluralize(@category.posts.count, 'Post') %></h3>
<ul>
  <% @category.posts.each do |post_obj| %>
    <li><%=link_to(post_obj.title, post_path(post_obj.id))%></li>
  <% end %>
</ul>
